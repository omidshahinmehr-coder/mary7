<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/tracker_screen_title"
        android:textSize="18sp"
        android:textStyle="bold"
        android:paddingBottom="12dp" />

    <!-- Part 1: log file viewer — bounded height (not squeezed to near-zero
         by the buttons below), with a sane minimum so it stays usable even
         on small screens. -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textStyle="bold"
        android:text="@string/tracker_log_view_header" />

    <TextView
        android:id="@+id/logFileNameText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="11sp"
        android:textColor="@color/color_text_hint"
        android:paddingTop="2dp"
        android:paddingBottom="6dp"
        android:text="@string/tracker_log_default_hint" />

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1.4"
        android:minHeight="120dp"
        android:background="@color/color_background">

        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <TextView
                android:id="@+id/logFileContentText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="8dp"
                android:textSize="12sp"
                android:fontFamily="monospace"
                android:text="—" />

        </ScrollView>

    </FrameLayout>

    <!-- Part 2: action buttons — bounded height (not wrap_content) so this
         area always scrolls to reveal every button, even on small screens,
         instead of overflowing off the bottom of the screen. -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="8dp"
        android:scrollbars="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <Button
                android:id="@+id/browseLogButton"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:minHeight="44dp"
                android:text="@string/tracker_browse_button" />

            <Button
                android:id="@+id/tripStatsButton"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:minHeight="44dp"
                android:layout_marginTop="6dp"
                android:text="@string/tracker_trip_stats_button" />

            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:background="@color/color_divider"
                android:layout_marginTop="14dp"
                android:layout_marginBottom="14dp" />

            <Button
                android:id="@+id/openTrackerSettingsButton"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:minHeight="44dp"
                android:text="@string/tracker_settings_button" />

            <Button
                android:id="@+id/openCommandHistoryButton"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:minHeight="44dp"
                android:layout_marginTop="6dp"
                android:layout_marginBottom="4dp"
                android:text="@string/tracker_command_history_button" />

        </LinearLayout>

    </ScrollView>

</LinearLayout>
